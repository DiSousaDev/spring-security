spring.profiles.active=${APP_PROFILE:test}
spring.jpa.open-in-view=false

security.token.secret=${APP_SECRET:secret}
security.token.issuer=${APP_ISSUER:Spring Security 3}

server.error.include-stacktrace=never